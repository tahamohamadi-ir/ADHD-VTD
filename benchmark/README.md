# Benchmark Directory

This directory contains benchmark definitions, experiment configs, baseline definitions, and evaluation protocols.

Do not store benchmark outputs here.

Benchmark outputs must be written to:

```text
results/benchmark/