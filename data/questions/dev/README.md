# پوشه `data/questions/dev`

این پوشه مجموعه توسعه است. هنگام تغییر دادن NLU، schema linker، SQL validator یا promptها، این split برای بازخورد سریع استفاده می‌شود.

از نظر آموزشی، `dev` جایی است که می‌توان فهمید یک تغییر واقعاً general شده یا فقط روی چند نمونه‌ی دستی خوب عمل کرده است.
