# پوشه `scripts/models`

این پوشه برای scriptهای مربوط به مدیریت مدل‌ها در نظر گرفته شده است.

در وضعیت فعلی فقط package marker دارد. اگر script دانلود، repair، integrity check یا registry sync برای مدل‌ها اضافه شود، باید اینجا قرار بگیرد یا به‌صورت روشن در `archive/scripts_legacy` نگه‌داری شود.
