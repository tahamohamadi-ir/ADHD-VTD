config_id: A2_schema_linking_positive400_bounded
ablation_id: A2_schema_linking_positive400_bounded
config_version: "paper1_ablation_positive400_bounded_v1"
mode: agent
description: "Paper-1 positive400 bounded A2: NLU plus schema linking, CAG/reflexion disabled."
dataset:
  split: positive400
  sample_size: 0
exclude_self: true
trace_level: compact
features:
  nlu: true
  schema_linking: true
  cag: false
  reflexion: false
  repair: false
  validation: true
  safety: true
  deterministic_templates: false
  max_retries: 1
reporting:
  result_status: ready_to_run
  paper_scope: "full positive400 ablation"
  implementation_note: "Schema-linking runtime support must be interpreted through stored module flags and traces."
