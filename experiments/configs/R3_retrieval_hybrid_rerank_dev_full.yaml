config_id: R3_retrieval_hybrid_rerank_dev_full
ablation_id: R3_retrieval_hybrid_rerank_dev_full
config_version: "phase11_retrieval_v1"
mode: retrieval
description: "Full-dev retrieval ablation: hybrid retrieval followed by the current identity reranker placeholder."
dataset:
  split: dev
retrieval:
  backend: hybrid_rerank
  top_k: 3
exclude_self: true
trace_level: compact
features:
  retrieval_backend: hybrid_rerank
  reranker: identity
reporting:
  result_status: config_only_not_a_result
  anti_fake: "This config is not a result until scripts/run_benchmark.py writes a real artifact."
  anti_leakage: "Keep exclude_self=true for dev/test."
  scope: "Full dev split; no sample or samples_per_level limit."
  limitation: "The current reranker is identity; this config verifies wiring only and is not a model-backed reranker claim."
