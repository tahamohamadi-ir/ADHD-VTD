config_id: R2_retrieval_hybrid_dev_full
ablation_id: R2_retrieval_hybrid_dev_full
config_version: "phase11_retrieval_v1"
mode: retrieval
description: "Full-dev retrieval ablation: BM25 plus vector hybrid retrieval."
dataset:
  split: dev
retrieval:
  backend: hybrid
  top_k: 3
exclude_self: true
trace_level: compact
features:
  retrieval_backend: hybrid
reporting:
  result_status: config_only_not_a_result
  anti_fake: "This config is not a result until scripts/run_benchmark.py writes a real artifact."
  anti_leakage: "Keep exclude_self=true for dev/test."
  scope: "Full dev split; no sample or samples_per_level limit."
