# پوشه `tests/fixtures`

این پوشه داده‌های کوچک و کنترل‌شده برای تست را نگه می‌دارد.

Fixture خوب باید کوچک، خوانا و هدفمند باشد؛ مثلاً یک schema کوچک، چند پرسش فارسی یا چند SQL نمونه برای یک validator خاص. استفاده از دیتاست کامل در unit test معمولاً تست را کند و شکننده می‌کند.
