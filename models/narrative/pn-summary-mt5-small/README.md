# مدل `pn-summary-mt5-small`

این پوشه مدل خلاصه‌سازی/روایت فارسی را نگه می‌دارد.

در این پروژه، narrative فقط باید بعد از اجرای SQL معتبر و با اتکا به result واقعی ساخته شود. این مدل نباید برای ساخت داده، تشخیص پزشکی یا نتیجه‌گیری خارج از result استفاده شود.
