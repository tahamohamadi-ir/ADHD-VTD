# پوشه `1_Pooling`

این پوشه config pooling مدل `paraphrase-multilingual-mpnet-base-v2` را نگه می‌دارد.

Pooling روی embedding نهایی اثر می‌گذارد؛ تغییر آن باید با benchmark retrieval و ثبت در registry همراه باشد.
