# پوشه `models/embeddings/sentence-transformers`

این پوشه مدل‌های embedding خانواده SentenceTransformers را نگه می‌دارد.

مدل‌های داخل این مسیر معمولاً شامل config، tokenizer، وزن‌ها و `1_Pooling` هستند. مسیر فعال باید در `MODEL_REGISTRY.md` مشخص شود.
