# پوشه `models/embedding`

این پوشه برای مسیر singular قدیمی/سازگاری مدل‌های embedding نگه داشته شده است.

اگر مدل embedding فعال استفاده می‌شود، ترجیحاً مسیر canonical آن را در `MODEL_REGISTRY.md` مشخص کنید تا `embedding/` و `embeddings/` باعث ابهام نشوند.
