# پوشه `models/rerankers`

این پوشه مسیر alternate/legacy برای مدل‌های reranker است.

اگر مدل reranker فعال بین `reranker/` و `rerankers/` وجود دارد، مسیر دقیق و وضعیت آن باید در `MODEL_REGISTRY.md` ثبت شود. پوشه‌های `.bad_*` نشان‌دهنده دانلود یا repair ناموفق هستند.
