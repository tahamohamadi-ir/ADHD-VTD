# مدل `bge-reranker-base`

این پوشه مدل reranker پایه BGE را نگه می‌دارد.

Reranker بعد از retrieval اولیه استفاده می‌شود تا candidateهای context بهتر رتبه‌بندی شوند. اثر آن باید جدا از generation با retrieval metrics سنجیده شود.
